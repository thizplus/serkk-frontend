// Search Feature Barrel Export

// Hooks
export {
  useSearch,
  useInfiniteSearch,
  usePopularSearches,
  useSearchHistory,
  searchKeys
} from './hooks/useSearch';

// Types (re-export shared types for convenience)
export type {
  SearchParams
} from '@/types';

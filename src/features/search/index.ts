// Search Feature Barrel Export

// Hooks
export {
  useSearch,
  usePopularSearches,
  useSearchHistory,
  searchKeys
} from './hooks/useSearch';

// Types (re-export shared types for convenience)
export type {
  SearchParams
} from '@/types';
